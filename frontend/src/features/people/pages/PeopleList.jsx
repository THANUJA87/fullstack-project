import { useState } from 'react';
import Modal from '../../../components/common/Modal';
import PeopleTable from '../components/PeopleTable';
import { userSchema, validationMessage } from '../../../validation/schemas';
import peopleService from '../people.service';

export default function PeopleList({ user, users, tenants, permissions, onRefresh }) {
  const [open, setOpen] = useState(false); const [editing, setEditing] = useState(null); const [error, setError] = useState(''); const role = user.role === 'SUPER_ADMIN' ? 'ADMIN' : 'AGENT';
  async function create(event) { event.preventDefault(); const form = new FormData(event.currentTarget); const result = userSchema.safeParse({ name: form.get('name'), email: form.get('email'), password: form.get('password'), tenantId: user.role === 'SUPER_ADMIN' ? form.get('tenantId') : user.tenantId, permissions: permissions.map((item) => item.key).filter((key) => form.get(`perm-${key}`) === 'on') }); if (!result.success) { setError(validationMessage(result)); return; } try { await peopleService.create({ ...result.data, role }); setOpen(false); await onRefresh(); } catch (err) { setError(err.message); } }
  async function toggle(target) { try { await peopleService.updateStatus(target.id, !target.isActive); await onRefresh(); } catch (err) { setError(err.message); } }
  async function save(event) { event.preventDefault(); const selected = permissions.map((item) => item.key).filter((key) => event.currentTarget.elements[`perm-${key}`]?.checked); try { await peopleService.setPermissions(editing.id, selected); setEditing(null); await onRefresh(); } catch (err) { setError(err.message); } }
  return <><section className="page-heading"><div><p className="eyebrow">Team management</p><h1>People</h1><p className="muted">{user.role === 'SUPER_ADMIN' ? 'Manage all users across tenants.' : 'Create and manage Agents in your workspace.'}</p></div><button className="primary-button" onClick={() => setOpen(true)}><span>＋</span> Create {role === 'ADMIN' ? 'admin' : 'agent'}</button></section>{error && <p className="banner-error">{error}</p>}<PeopleTable users={users} onToggle={toggle} onPermissions={setEditing} />{open && <Modal title={`Create ${role.toLowerCase()}`} onClose={() => setOpen(false)}><form onSubmit={create}><label>Name<input name="name" required /></label><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" minLength={8} required /></label>{user.role === 'SUPER_ADMIN' && <label>Tenant<select name="tenantId" defaultValue=""><option value="">Unassigned</option>{tenants.map((tenant) => <option key={tenant.id} value={tenant.id}>{tenant.name}</option>)}</select></label>}{role === 'AGENT' && <fieldset className="perm-fieldset"><legend>Initial permissions</legend>{permissions.map((item) => <label className="checkbox-label" key={item.key}><input type="checkbox" name={`perm-${item.key}`} />{item.label}</label>)}</fieldset>}<button className="primary-button" type="submit">Create user</button></form></Modal>}{editing && <Modal title={`Permissions — ${editing.name}`} onClose={() => setEditing(null)}><form onSubmit={save}><fieldset className="perm-fieldset">{permissions.map((item) => <label className="checkbox-label" key={item.key}><input type="checkbox" name={`perm-${item.key}`} defaultChecked={editing.permissions?.includes(item.key)} />{item.label}</label>)}</fieldset><button className="primary-button" type="submit">Save permissions</button></form></Modal>}</>;
}
