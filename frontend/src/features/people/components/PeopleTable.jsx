import StatusBadge from '../../../components/common/StatusBadge';
export default function PeopleTable({ users, onToggle, onPermissions }) { return <div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Workspace</th><th>Status</th><th>Permissions</th><th /></tr></thead><tbody>{users.map((user) => <tr key={user.id}><td><strong>{user.name}</strong></td><td>{user.email}</td><td><span className="role-badge">{user.role.replace('_', ' ')}</span></td><td>{user.tenantName || '—'}</td><td><StatusBadge active={user.isActive}>{user.isActive ? 'Active' : 'Disabled'}</StatusBadge></td><td>{user.role === 'AGENT' ? (user.permissions?.join(', ') || 'View only') : 'Full access'}</td><td className="actions-cell">{(user.role === 'AGENT' || user.role === 'ADMIN') && <button className="text-button" onClick={() => onToggle(user)}>{user.isActive ? 'Disable' : 'Enable'}</button>}{user.role === 'AGENT' && <button className="text-button" onClick={() => onPermissions(user)}>Permissions</button>}</td></tr>)}</tbody></table></div>; }
